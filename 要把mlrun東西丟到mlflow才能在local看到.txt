cp -r ./mlruns/ ..//MLflow/
然後開MLflow的vscode指令mlflow ui --port 8080開


#
with mlflow.start_run(run_name=run_name):#mlflow
    mlflow.tensorflow.autolog()#mlflow tensorflow  

mlflow.log_param("n", n) #mlflow

run_id = mlflow.active_run().info.run_id#mlflow
print(f"Model saved in run {run_id}")#mlflow


#mlflow.log_metric("Train score", try.score(X_train, y_train))#mlflow
#mlflow.log_metric("Test score", try.score(X_test, y_test))#mlflow



    mlflow.sklearn.log_model(     #mlflow.sklearn.log_model() #紀錄sklearn模型
        sk_model=sk_model, 
        artifact_path="artifact_path",
        registered_model_name='registered_model_name',  #
    )   